@model Mines_Web.Models.BoardModel

<div class="timer">60</div>

@for (var y = 0; y < Model.ColumnSize; y++)
{
    for (var x = 0; x < Model.RowSize; x++)
    {
        <button value="@(x + " " + y)" class="cell-button"></button>
    }
    <br />
}