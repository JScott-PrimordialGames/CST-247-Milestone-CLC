@model List<Mines_Web.Models.GameObject>

    @for (int i = 0; i < Model.Count; i++)
    {
        <div class="saved-game-box">
            <p>DateCreated: @Model[i].DateCreated   </p>
            <p>ID: @Model[i].Id  </p>
            @Html.ActionLink(
                 "Load",
                 "LoadGame",
                 "GameCenter",
                 new { gameID = Model[i].Id },
                 null
             )
        </div>
    }

